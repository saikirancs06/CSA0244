#include<stdio.h>
int main()
{
	int a,b;
	
	printf(" enter a  value:");
	scanf("%d",&a);
	printf(" enter b value:");
	scanf("%d",&b);
	
	 int add=a+b;
	int sub=a-b;
	 int mul=a*b;
	printf("add:%d\n",add) ;
	printf("sub:%d\n",sub) ;
	printf("mul:%d\n",mul) ;
	
	 
	 
	 
	 return 0;
}
